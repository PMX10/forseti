package Forseti;

struct ConfigData {
  string ConfigFile;
  boolean IsBlueAlliance;
  int32_t TeamNumber;
  string TeamName;
  int32_t PiEMOSNumber;

  // Might be deprecated in favor of defining an LCM format.
  string FieldObjects;
}
